// OnPad, whenever the Pad is turned on and different from another padcap
    // one-shot trigger note with existing parameters
    // set step as current step
    // Record stale knob positions

// On Knob movement
    // if knob moves from stale knob position, record value to step

// On Step
    // Output values from step memory

// Control Loop
    // if Play knob > play FWD play threshold, enable playback FWd
    // if Play knob < play REV play threshold, enable playback reverseKnobs
